# Previous experiments

This directory preserves earlier experimental work for traceability.

The current application architecture is defined by the top-level README, `ARCHITECTURE.md`, `face_pipeline/`, `model_runtime/`, `api/`, and `extension/`. Files here should be treated as historical references rather than the active implementation contract.
