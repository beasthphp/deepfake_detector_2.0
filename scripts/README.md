# Developer utilities

This directory contains verification and maintenance scripts used around the local deepfake MVP.

The scripts cover tasks such as model verification, repository safety checks, extension validation, and reproducible evaluation helpers. They are intentionally kept outside the runtime packages so operational tooling does not become part of the API or model-provider contract.
