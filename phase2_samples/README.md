# Phase 2 sample outputs

This directory contains sample artifacts used to sanity-check face detection and crop behavior during the preprocessing phase of the project.

They are development references for the face pipeline, not a benchmark dataset and not evidence of model accuracy.
